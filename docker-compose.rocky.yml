version: '3.8'

services:
  vnc-rocky:
    build:
      context: .
      dockerfile: Dockerfile.rocky
    ports:
      - "6080:6080"
      - "5999:5999"
    environment:
      - DISPLAY=:99
    volumes:
      - ./screenshots:/app/screenshots
    command: /app/run_test_rocky.sh
